#2.0 Creating your first React Component 

Component는 HTML을 반환하는 함수

jsx는 javascript 안의 HTML 

예전에는 react application은 한 개의 컴포넌트만 랜더링이 가능했지만 지금은 여러개의 컴포넌트를 랜더링 할 수 있음

ex) ReactDOM.render(
  <React.StrictMode>
    <App /><Tomato /><../>
  </React.StrictMode>,

#2.1 Reusable Components with JSX + Props 

reac는 재사용 가능한 component를 만들 수 있음, 계속해서 반복해서 만들 수 있음

Component는 대문자로 시작해야하고 component를 통해서 정보를 보낼 수 있음

#2.2 Dynamic Component Generation

map 함수 array의 각 item에 function을 적용하고 array를 줌

따라서 map은 array을 취하고 우리가 정확히 원하는 array를 반환해 줌